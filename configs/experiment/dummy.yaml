# @package _global_

# To execute this expriment runs:
# python train.py experiment=dummy

defaults:
  # - override /callbacks: default.yaml
  # - override /trainer: default.yaml

tags: ["dev"]

seed: 12345

trainer:
  min_epochs: 10
  max_epochs: 10
  gradient_clip_val: 0.5

model:
  optimizer:
    lr: 0.002

data:
  batch_size: 64

logger: null
